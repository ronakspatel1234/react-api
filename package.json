{
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "Simple Fake API",
  "main": "main.js",
  "scripts": {
    "start": "json-server --watch ./database.json --host --port 8080",
    "start-auth": "node server.js --host"
  },
  "author": "ME:)",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.1",
    "jsonwebtoken": "^9.0.0"
  }
}
